[project]
name = "jetson-llm-mcp"
version = "0.1.0"
description = "MCP server for Jetson AGX Xavier local LLMs"
requires-python = ">=3.10"
dependencies = ["mcp>=1.0", "httpx>=0.27"]

[project.scripts]
jetson-llm-mcp = "server:mcp"
